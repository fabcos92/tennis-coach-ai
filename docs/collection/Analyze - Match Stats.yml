info:
  name: Analyze - Match Stats
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:8080/analyze
  body:
    type: json
    data: |-
      {
        "type": "match_stats",
        "stats": {
          "first_serve_pct": 58,
          "second_serve_won_pct": 32,
          "unforced_errors": 25
        }
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
